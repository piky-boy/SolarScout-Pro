# SolarScout Pro — Project Memory

## Overview
SolarScout Pro is a solar lead-generation SaaS for commercial rooftop prospects in **Romania, Spain, Portugal, Albania, and the United Kingdom (GB)**. It combines satellite building detection with a lead pipeline and rooftop solar potential analysis.

- **Project root**: `/home/ubuntu/solarscout_pro`
- **Production URL**: https://solarscout-pro-7foyl3.abacusai.app
- **Tech**: Next.js 14 App Router · TypeScript · Prisma + PostgreSQL (Abacus-hosted) · NextAuth v4 · Mapbox GL JS · Tailwind + shadcn/ui.

## Current integrations
- **Mapbox** (maps + geocoding) — env: `MAPBOX_TOKEN`, `NEXT_PUBLIC_MAPBOX_TOKEN`. Forward geocoding is country-restricted to `ro,es,pt,al,gb`.
- **Overpass / OpenStreetMap** — building polygons + roof area computation.
- **Google SSO** — env: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`. Google Cloud project is "SolarScout Pro" (project number 1050055939275). OAuth redirect URIs configured for `solarscout-pro-7foyl3.abacusai.app` and the preview host.
- **Google Solar API** — `buildingInsights:findClosest`. Key is `GOOGLE_MAPS_API_KEY` (shared key, works across Maps Platform). Auto-retries with `experiments=EXPANDED_COVERAGE` on 404. Coverage in our markets: **UK = HIGH**, Spain/Portugal = MEDIUM, Romania/Albania often not indexed.
- **Google Places API (New)** — `places:searchNearby`. Same `GOOGLE_MAPS_API_KEY`. **Enabled in project as of 2026-04-18** — returns real Romanian / Albanian businesses with phone, website, rating, opening hours. The code still uses `Promise.allSettled` so transient 403s fall through safely.
- **Abacus.AI LLM API** (`apps.abacus.ai/v1/chat/completions`) — env: `ABACUSAI_API_KEY`. Model `gpt-5.4-mini` with `response_format: json_schema`. Used exclusively by the outreach-kit endpoint. Never exposed to the client — all calls go server-side through `/api/leads/[id]/outreach`.
- **Abacus.AI HTML→PDF API** — used by `/api/leads/[id]/proposal` to render a branded 2-page PDF. No extra env vars beyond `ABACUSAI_API_KEY`. Polls `getConvertHtmlToPdfStatus` for up to 110s; `maxDuration=120`.
- **Abacus.AI Notification email API** — env: `WEB_APP_ID=12b2781eaf`, `NOTIF_ID_LEAD_OUTREACH_EMAIL=127b27b821`. Notification type registered as "Lead Outreach Email" (USER recipient, non-critical, unsubscribable). Sender alias: `{senderName} · {senderCompany}`; sender_email: `noreply@{hostname(NEXTAUTH_URL)}`. Deliverability caveat: for better inbox placement, user can configure custom domain email in Settings → Notifications after deploying on their custom domain.

## Design language
- Primary accent: **amber-500/600**. Everything "solar" or "interactive" is amber.
- Secondary colors: emerald for OSM badges, blue for Google, violet for HYBRID.
- Fonts: `DM_Sans` display, `Plus_Jakarta_Sans` body, `JetBrains_Mono` for coordinates / tabular.
- Cards with subtle gradients, generous whitespace, shadow-lg for focal elements.
- Dark mode supported throughout; always include `dark:` variants for color tokens.

## Key files
- `lib/db.ts` — Prisma client singleton.
- `lib/auth.ts` — NextAuth options (Google + credentials).
- `lib/mapbox.ts` — forward geocoding (country-restricted to RO/ES/PT/AL).
- `lib/overpass.ts` — Overpass API commercial-building discovery → returns `OsmBuilding[]` with polygons/roof area.
- `lib/places.ts` — Google Places (New) search, classification, haversine matching. Exports `isPlacesApiConfigured`, `searchNearbyPlacesFanout`, `haversineMeters`.
- `lib/solar.ts` — Solar API client with caching statuses OK/NOT_FOUND/NO_COVERAGE/UNCONFIGURED/ERROR.
- `app/api/leads/generate/route.ts` — Hybrid OSM + Places discovery. Matches OSM buildings to Places by <40m proximity → HYBRID. Unmatched Places become GOOGLE_PLACES (only in OSM-thin markets RO/AL or when OSM found zero).
- `app/api/leads/[id]/solar/route.ts` — Solar data endpoint with 30-day cache.
- `app/api/leads/[id]/outreach/route.ts` — POST-only. Takes `{ language, tone }`, reads the lead + cached solar fields, computes a deterministic business case (see `lib/outreach.ts`), then calls the LLM with a strict JSON schema (`outreach_kit`) to produce `{ email: {subject, body}, call: {opener, keyPoints[], objectionResponse, closing}, whatsapp }`.
- `lib/outreach.ts` — Deterministic economics (system kWp, annual production kWh, self-consumption 70%, €900/kWp install cost, country-specific electricity tariffs ES 0.22 / PT 0.20 / RO 0.18 / AL 0.16 / GB 0.28 €/kWh, 25-yr lifetime). Country-specific specific yield: Iberia ~1,400 kWh/kWp/yr, RO/AL ~1,150, GB ~950 (lower irradiance). Also exports `SUPPORTED_LANGUAGES` (en/es/pt/ro/sq) and `OUTREACH_TONES` (professional/friendly/direct) and `defaultLanguageForCountry` (GB→en).
- `app/leads/[id]/_components/outreach-generator.tsx` — The UI. Gradient violet/amber header, language + tone selects, business-case banner with 4 KPIs, tabs for Email / Call script / WhatsApp. Each tab has editable content, copy buttons, and a direct-action button (mailto:, tel:, wa.me/).
- `lib/proposal-html.ts` — 2-page A4 HTML template (inline CSS, Mapbox static satellite image) for proposal PDF. Uses same `BusinessCase` from `lib/outreach.ts`.
- `app/api/leads/[id]/proposal/route.ts` — POST endpoint. Computes business case, renders HTML, calls HTML→PDF API with polling, returns PDF with `Content-Disposition: attachment`.
- `app/leads/[id]/_components/proposal-button.tsx` — Dialog with sender name/company/email/phone inputs. Persists to localStorage via `useEffect` (NEVER in useState init — causes hydration bug). Triggers blob download.
- `app/api/leads/outreach/send/route.ts` — Bulk email send. Max 50 per batch. Serial processing. Builds HTML body from each `{subject, body}` item, replaces `[Your name]`/`[Your company]` placeholders, appends signature with sender email + unsubscribe line, calls `sendNotificationEmail` for each lead, auto-sets `lead.status = 'CONTACTED'` on success. Returns per-lead `{results: [{leadId, ok, error?}]}`.
- `app/leads/_components/bulk-outreach-dialog.tsx` — Multi-phase dialog (setup → generating → review → sending → done). Iterates selected leads with serial LLM calls, progress bar, per-lead success/error badges. Editable subject+body per lead with checkbox to include/exclude. GDPR banner at top. Persists sender prefs to localStorage via useEffect.

## Data model (Lead)
Core business fields: businessName, businessType, buildingType, address, city, country, latitude, longitude, roofAreaSqm, contactPhone, contactEmail, website, notes, status.

Identity: `osmId` (nullable, unique per user when set), `googlePlaceId` (nullable, app-enforced uniqueness per user).

Data source: `dataSource` = `'OSM' | 'GOOGLE_PLACES' | 'HYBRID'` (default 'OSM').

Google Places extras: `openingHours` (JSON), `googleRating`, `googleRatingCount`.

Solar cache (12 fields): `solarDataFetchedAt`, `solarApiStatus`, `solarImageryQuality`, `solarImageryDate`, `solarMaxPanelCount`, `solarMaxArrayAreaSqm`, `solarMaxSunshineHours`, `solarYearlyEnergyKwh`, `solarCarbonOffsetKgYr`, `solarPanelCapacityWatts`, `solarPanelLifetimeYears`, `solarRawJson`.

Indexes: userId, country, city, businessType, createdAt, solarApiStatus, dataSource, googlePlaceId.

## Behavior & UX rules
- **Never** pull `solarRawJson` in list queries — it can be huge. Always use explicit `select` in the leads list.
- Always serialize Date fields to ISO strings before passing to client components.
- OSM remains primary source for Spain/Portugal/UK (`OSM_PRIMARY_COUNTRIES = {ES, PT, GB}` in `app/api/leads/generate/route.ts` — these markets have dense OSM building coverage); Places enriches with contact/rating.
- In Romania/Albania, Places-only leads are allowed since OSM is thin.
- The data source badge (OSM green / Google blue / Hybrid violet) appears on both the leads table and the detail page.
- Places API 403 / disabled just falls through silently via `Promise.allSettled` — never blocks an OSM-only search.

## User style & preferences
- Direct, action-oriented. Prefers clear wins delivered with context.
- Wants honest technical advice (e.g., "don't swap Mapbox just because" recommendations).
- Receptive to cost/pricing awareness when suggesting new API integrations.

## Test credentials
- Seeded admin: `john@doe.com` / `johndoe123` (scripts/seed.ts).
- Google SSO also works once logged in to a Google account.

## Known caveats
- `yarn prisma db push --accept-data-loss` must never be run. If unique constraints are needed on a new field, enforce uniqueness in application code (e.g., via `findFirst` + create/update pattern we use for `googlePlaceId`).
- Places API (New) must be enabled in Google Cloud Console project 1050055939275 before the hybrid discovery returns Google results.
- The `place` key (GOOGLE_MAPS_API_KEY) should be restricted (HTTP referrers + API restrictions: Solar API, Places API New, later Geocoding API if needed).
